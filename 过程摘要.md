# QuietBite 项目过程摘要

> 状态：阶段性简要记录  
> 时间范围：2026-09-17 至 2026-09-19  
> 整理方式：根据当前对话、代码、Spec、测试日志和验收记录重建；不是逐字聊天记录。  
> 目的：保留产品演变、技术判断、开发问题和解决思路，后续可按章节继续展开。

## 1. 项目起点

题目的核心要求是：用户正在正常使用自己的手机时，Agent 同时在同一台手机上完成一个真实任务，而且不能抢屏幕、焦点、键盘或打断用户。

用户此前主要有 Web 开发经验，没有做过手机 Agent，因此项目最初没有直接进入编码，而是先理解手机 Agent 的常见实现方式和它们与 Web Agent 的差异。

我们逐步形成的关键认识是：

- 这道题最难的不是“让模型会点手机”，而是解决用户与 Agent 对屏幕、焦点和输入法的资源冲突。
- 常见手机 GUI Agent 默认“一块屏幕同一时刻只有一个操作者”，与本题要求天然冲突。
- 如果 Agent 不操作前台 GUI，而是复用 iOS 已有的后台能力和系统动作，就有机会绕开资源争抢。
- 因此，本项目把重点从“模仿人点击手机”转向“通过系统入口提交任务，在后台完成任务，再静默交付结果”。

---

## 2. 阶段一：学习参考资料，建立手机 Agent 认知

### 用户要求

> “我没有做过手机 agent 相关的项目，制作过 Web 端的，给我解释一下 scrcpy、AutoGLM Phone 和 Awesome LLM-Powered Phone GUI Agents。”

### 我们的理解

先帮助用户理解手机控制、视觉模型决策和 GUI Agent 研究现状，再讨论项目方案，避免一开始就被常见方案限制。

### 实际行动

- 分析 `scrcpy` 的 ADB、屏幕采集、视频传输和输入注入机制。
- 解释 AutoGLM Phone 的“截图/界面状态 → 视觉语言模型 → 动作 → 新状态”循环。
- 介绍 Awesome Phone GUI Agents 仓库作为论文、数据集、基准和开源实现的索引，而不是一个可以直接部署的完整产品。

### 关键发现

- `scrcpy` 擅长远程显示和控制，但控制的仍是用户正在看的前台界面。
- AutoGLM 一类 GUI Agent 能理解界面并执行点击、滑动和输入，但通常仍会改变前台画面和焦点。
- 现有研究多数关注“Agent 能否替人完成 GUI 操作”，本题更关注“Agent 与用户能否同时使用一台手机”。

### 用户反馈

用户继续追问如何实现“不影响用户使用”，说明关注点已经从参考项目本身转向本题的差异化架构。

### 复盘

做对的地方：没有直接把 scrcpy 或 AutoGLM 当作答案，而是先识别它们与题目核心要求的冲突。  
需要展开的地方：后续可补充 Android 虚拟显示、多用户/Profile、Accessibility 和 iOS 后台能力的系统级比较。

---

## 3. 阶段二：提出“不抢前台”的产品架构

### 用户要求

> “我们现在来商讨一下这次项目的规划，如何能实现手机 Agent 在运行的时候不影响用户使用。”

### 我们的理解

需要把 Agent 的输入、运行位置和结果交付都从前台 GUI 操作中解耦。

### 实际行动

形成了三段式架构：

1. iPhone 使用 Shortcut 作为轻量入口，取得用户输入、当前位置和当前时间。
2. Windows 本地服务作为 Agent 的运行环境和“大脑”，执行模型调用、联网检索和确定性校验。
3. iOS Notes 等系统能力作为静默结果出口，任务结束后用户自行查看。

### 关键发现

- Shortcut 可以利用 iOS 已有的输入、位置、网络请求和 Notes 动作，不需要模拟点击。
- Agent 的主体不必运行在手机前台；题目允许连接电脑，“手机做手脚、大脑在云端或电脑”符合交付范围。
- 只要 Shortcut 提交后不再打开 App、弹窗或展示结果，用户就能继续使用原来的前台应用。

### 用户反馈

用户确认前端本质上是在组合 iOS 生态已经提供的功能和交互，并继续询问 Shortcut 如何启动和运行。

### 复盘

做对的地方：抓住了系统能力组合这一条与 GUI Agent 不同的路线。  
遗漏：最初没有立即把“无通知、无 Quick Look、无 Open App、完成回调后才算完成”写成严格验收条件，后来才补齐。

---

## 4. 阶段三：从 QuietTrip 转向 QuietBite

### 用户要求

最初方案是 `QuietTrip`：通过 Shortcut 提交行程相关需求，由后台处理并写入日历、提醒或备忘录。随后用户提出：

> “Agent 做的事太普通了或者太简单了，只是简单创建了一些日历备忘录。”

### 我们的理解

题目不能只证明“后台能写一个系统对象”，还应展示 Agent 的理解、检索、判断和证据处理能力。

### 实际行动

将产品改为 `QuietBite`：一个不抢手机前台的本地餐厅调研 Agent。

示例任务：

```text
今晚 7 点两个人想吃川菜，人均 150 元以内，不吃内脏，
帮我在当前位置附近找几家靠谱餐厅。
```

Agent 需要解析时间、人数、菜系、预算和偏好，搜索真实门店，核对公开来源，排序后将结果写入 Notes。

### 关键发现

- 创建 Notes 只是交付动作，真正的产品能力变成了约束理解、联网调研、证据校验、实体去重和可解释排序。
- 餐厅推荐比简单日历创建更能展示 Agent 的推理和工具使用能力。
- 任务仍然可以完全在后台完成，不需要操作大众点评或地图 App 的前台界面。

### 用户反馈

用户批准 QuietBite Spec，并持续根据真实运行结果调整证据标准、输出内容和候选数量。

### 复盘

做对的地方：根据用户对“任务价值”的质疑及时更换了产品核心，而不是继续堆叠日历功能。  
需要注意：餐厅数据天然存在时效性和平台访问限制，真实数据质量后来成为主要难点。

---

## 5. 阶段四：先写 Spec，再进行开发

### 用户要求

> “写一份专门为 Luna 模型作为本项目子 Code Agent 用于代码开发的 Spec 文档……我审批同意后才可以创建子 Agent 进行开发。”

附加要求包括：README 要有技术选型说明，测试必须使用真实手机，只有真实场景通过才算通过。

### 我们的理解

需要先固定产品边界、API、状态机、证据标准、测试案例和交付标准，避免代码 Agent 因设计不清反复返工。

### 实际行动

- 创建并迭代 `QUIETBITE_SPEC.md`。
- 规定异步 Job API、Bearer Token、幂等 `request_id`、状态轮询和完成回调。
- 规定自动化测试只能标记 `CHECK_OK/CHECK_FAILED`，不能冒充真机 `PASS`。
- 创建 `README.md`、`.env.example`、Shortcut 配置文档和真机验收模板。
- 在批准后进行代码开发，主 Agent 保留架构决策和验收责任。

### 关键发现

- 先定义“什么才算完成”比直接写代码重要，尤其是 `[DONE]` 必须发生在 Notes 真实创建回调之后。
- Mock 可以验证代码分支，但不能证明真实搜索、真实 Notes 或用户前台不受干扰。
- 用户相关事项可以放入待办，不应阻塞其余代码和文档工作。

### 用户反馈

用户批准开发，并要求尽可能保持任务运行，将需要人工参与的事项集中写入待办。

### 复盘

做对的地方：建立了清晰的自动化与真机验收边界。  
做错或过度设计的地方：初版 Spec 对餐厅证据要求过严，假设搜索摘要可以稳定提供所有字段，后续被真实数据推翻。

---

## 6. 最终技术选型及理由

| 模块 | 选择 | 结合本项目情况的理由 |
|---|---|---|
| 手机入口 | iOS Shortcut | 开发周期短；无需原生 App 签名；可原生获取输入、位置和时间；提交后可静默结束 |
| Agent 运行位置 | Windows 本地服务 | 用户已有 Web/后端开发基础；题目允许连接电脑；便于查看日志、调试模型和录制演示 |
| 后端语言 | Python 3.11 | 模型和 HTTP 调用实现直接；Windows 部署简单；适合一周内完成 MVP |
| HTTP 服务 | `ThreadingHTTPServer` | 演示规模小；标准库足够；无需引入和部署 Web 框架 |
| 任务协议 | POST 创建任务 + GET 轮询 + POST 完成回调 | 首次请求可以快速返回；搜索在后台运行；Notes 真实创建后才能确认完成 |
| 意图与事实提取 | `glm-5.3-flash` | 将中文自然语言转成固定 JSON，并从搜索摘要中提取有来源的事实 |
| 联网数据 | 智谱 Web Search API `search_pro` | 能返回标题、URL 和摘要；无需自己维护通用爬虫；可保留来源 |
| 排序与过滤 | 本地确定性 Python 代码 | 防止 LLM 随意改变预算判断、营业判断和最终名次 |
| 结果交付 | iOS Notes | 可以由 Shortcut 静默创建；方便演示后人工查看；不会抢当前前台 App |
| 状态存储 | 内存字典 | 单机演示和少量任务足够；避免为了演示引入数据库运维 |
| 隐私 | Windows 先脱敏位置 | 精确门牌不进入模型、搜索请求、日志或仓库 |

### 为什么没有选择其他方案

| 未选择方案 | 原因 |
|---|---|
| scrcpy / ADB 输入注入 | 会操作用户正在看的前台屏幕，与用户争夺触控和焦点；iPhone 也不适用 ADB |
| AutoGLM / 视觉 GUI Agent | 依赖截图、点击、滑动和输入循环，适合替用户操作，不适合与用户同时操作同一前台 |
| 原生 iOS App | 一周 MVP 的开发、签名、权限和部署成本更高；第一版 Shortcut 已覆盖必要系统能力 |
| Web 前端 | 用户真正需要的入口在手机；额外 Web 页面不能解决 iPhone 位置和 Notes 静默写入问题 |
| 高德路线 API | 当前任务不承诺路线、距离或到达时间；引入 Key 和结构化 POI 会扩大范围 |
| 直接抓取大众点评页面 | 登录、动态渲染、反爬、合规和稳定性风险高，不适合面试演示 |
| 数据库和复杂框架 | 当前只有单机、少量并发和短生命周期任务，内存状态和标准库已经足够 |

---

## 7. 阶段五：Shortcut 真机配置与通信排障

### 用户要求

用户按照文档在真实 iPhone 上逐步配置 UUID、带时区时间、首次 POST、轮询、Notes 创建和完成回调，并不断反馈界面差异和错误。

### 我们的理解

需要把抽象流程翻译成中文 iOS Shortcut 中可以逐项点击的动作，并用真实服务验证。

### 实际行动

- 明确 UUID、ISO 8601 时间、JSON 字段和变量类型。
- 解释 `job_id` 是后台任务标识，而不是产品名称。
- 搭建轮询框架，处理 `ready/rejected/failed/completed`。
- 配置 Notes 创建结果和 `/complete` 回调。
- 排查并修复 Bearer Token 不一致、轮询 GET 缺少鉴权等问题。

### 关键发现

- iOS 版本和中文界面会导致动作名称与文档不同，例如 UUID、哈希和条件类型的显示差异。
- 首次 POST、后续 GET 和完成回调必须复用完全相同的 Authorization Header。
- Shortcut 的变量可能需要显式转成文本后才能与 `ready` 比较。

### 用户反馈

用户先后完成健康检查、首次提交和轮询配置；最终 0.1.7 在真实 iPhone 上创建 Notes，并在回调后输出 `[DONE]`。

### 复盘

做对的地方：始终以用户截图和真实日志为准，而不是假设 Shortcut 界面一致。  
遗漏：早期配置说明仍不够细，导致 Token、条件类型和回调分支多次往返排查。

---

## 8. 阶段六：搜索结果、时间和证据门槛排障

### 用户要求

真实运行中陆续出现 `INVALID_TIME`、`search sources=0`、上游超时和 `NO_VERIFIABLE_CANDIDATES`，用户要求继续排查直到能够产生真实结果。

### 我们的理解

需要区分请求格式、搜索响应解析、模型抽取和本地证据门槛四个不同层次的问题，不能只调整提示词。

### 实际行动

- 统一带时区 ISO 8601 时间并修正“今晚”等时间规则。
- 发现搜索结果位于 `tool_calls[*].search_result`，补充递归解析逻辑。
- 限制来源数量、摘要长度和模型输入，缓解超时。
- 增加拒绝原因与来源信号日志，定位具体缺失字段。
- 增加按具体门店名的二次补证搜索。

### 关键发现

- `sources=0` 最初不是互联网没有结果，而是代码只读取了错误的响应字段。
- 搜索来源越多不一定越好，过大的证据输入会造成模型超时。
- 初版严格要求每家同时具有营业时间、人均和质量依据，真实搜索摘要无法稳定满足，导致候选全部被拒绝。

### 用户反馈

用户提出：“如果无法返回的信息就不要展示了”，批准把不可稳定获得的字段改为可选。

### 复盘

做对的地方：通过结构化诊断日志逐层定位，而不是盲目放宽所有规则。  
做错的地方：初版证据模型是按理想数据源设计的，没有先用真实 Web Search 响应验证数据可得性。

---

## 9. 阶段七：从“严格战报”调整为“有证据才展示”

### 用户要求

用户认为备忘录中重复的“未确认字段”和排队说明太多；同时指出“不吃内脏”不应淘汰整家餐厅，只要不点相关菜即可，并要求最多展示证据最完整的 5 家。

### 我们的理解

结果应简洁、有用，不应把数据缺失包装成满足约束，也不应把普通口味偏好错误升级为整店安全过滤。

### 实际行动

- 营业时间、人均、评分、电话和推荐菜没有来源时直接省略。
- 将重复说明合并为备忘录末尾的一条全局提示。
- 普通忌口不再淘汰门店；严重过敏和绝对安全要求仍拒绝任务。
- 最多输出 5 家，并优先选择证据字段更完整的候选。

### 关键发现

- “不知道”与“符合要求”必须严格区分。
- 用户口味偏好和医疗安全约束不能使用同一过滤规则。
- 输出简洁度本身是产品质量的一部分。

### 用户反馈

真实运行成功返回 5 家并完成 Notes 创建；用户随后关注这些候选为何缺少评分、人均等更有价值的信息。

### 复盘

做对的地方：让产品从“看似信息很多”转向“只展示可以负责的信息”。  
待改进：只有名称和地址的候选仍然决策价值有限，因此需要更好的结构化餐厅数据源。

---

## 10. 阶段八：大众点评公开信息尝试

### 用户要求

> “如果是从大众点评上搜索现成的信息呢？因为大众点评就有评分、人均、营业时间、电话和推荐菜等信息。”

### 我们的理解

希望在不直接爬取或控制大众点评 App 的前提下，优先从公开搜索结果中补充质量字段。

### 实际行动

- 0.2.0：按具体门店名执行 `site:dianping.com` 搜索，只接受真实 `dianping.com` 子域。
- 真实日志显示 5 次补证全部 `sources=0`，因此本轮没有任何大众点评数据进入事实提取。
- 0.2.1：改用智谱正式参数 `search_domain_filter=www.dianping.com`，并同时记录 `raw_sources` 和过滤后的 `sources`。
- 保留安全回退：大众点评补证失败时仍使用首轮有来源的候选，不让整个任务失败。

### 关键发现

- 把 `site:` 写进普通搜索词不等于搜索服务保证站点过滤。
- 是否能获得大众点评字段，取决于搜索服务是否返回可访问的点评页面及其公开摘要是否包含字段。
- 即使页面存在，也不能在没有明确摘要证据时补写评分、人均或营业时间。

### 用户反馈

用户同意进行 0.2.1 尝试。随后确认首个“获取 URL 内容”卡住的问题已经解决，
RT-01 也已经完成；但手机型号、耗时、Notes 前后数量等验收明细仍待补录。
正式域名过滤返回的链接可以打开，但页面显示“暂无相关推荐”，因此本轮没有取得可用于
评分、人均、营业时间、电话或推荐菜的有效大众点评证据。

### 复盘

做对的地方：真实失败后没有伪造大众点评字段，并保留了原结果回退。  
做错的地方：0.2.0 应先查官方 API 参数，而不是先假设 `site:` 语法有效。  
后续判断：若正式域名过滤仍返回 0，应承认该数据路径不可用，再评估官方合作接口或其他结构化 POI 数据源，而不是无限调整提示词。

---

## 11. 阶段九：阻止单条评价冒充门店证据

### 用户要求

> “大众点评返回的链接都是没有信息的，我挑两个返回的结果发给你检查一下。”
>
> “实现下一版修改。”

### 我们的理解

0.2.1 只校验域名仍然过宽：`/review/<id>` 是单条用户评价，不是具体门店聚合页。
其中出现的个人消费或评分不能当作餐厅的人均、公开评分等门店事实；不同候选的补证
来源也不能混在一起由模型自由引用。

### 实际行动

- 将版本升级到 0.2.2，只接受 `dianping.com/shop/<id>` 形式的具体门店页。
- 明确拒绝 `/review/`、`/shopshare/`、分类页、仿冒域名和其他非门店路径。
- 要求门店页标题或摘要与当前候选店名匹配，并为来源增加 `candidate_scope`。
- 在本地证据校验中拒绝把甲店作用域的来源引用到乙店。
- 保留安全回退：只有评价页或没有匹配门店页时，继续使用首轮公开来源候选，
  但不把这些点评链接和字段写入 Notes。
- 日志新增 `shop_sources`，区分“搜索有返回”“存在门店页”“门店页匹配当前候选”。
- 新增评价页拒绝、候选错店拒绝、来源隔离和安全回退测试。

### 关键发现

- 域名正确不代表页面语义正确，URL 路径和实体归属都必须验证。
- 单条评价里的 `¥xxx/人` 是该用户一次消费线索，不能直接等价为门店聚合人均。
- 多家门店并发补证时，仅靠 Source ID 合法性不足，还必须保留来源所属候选。
- 搜索结果只包含无效点评页时，宁可不展示点评字段，也不能制造“已补证”的假象。

### 用户反馈

用户提供的火锅、日料、粤菜三次真实运行都产生了不同 Job、Notes 和 `[DONE]`，证明
任务会重新搜索而非复用固定结果；但人工打开两个 `/review/<id>` 链接后发现不能支持
对应门店聚合事实，因此要求继续修改。0.2.2 的真实模型和真机回归尚待用户执行。

### 复盘

做对的地方：基于用户打开真实链接后的证据继续收紧边界，没有把“域名过滤成功”当成
“事实核验成功”。  
做错的地方：0.2.1 只验证域名，遗漏了页面类型与候选实体归属，导致单条评价页可能被
当作门店质量来源。  
当前结果：0.2.2 自动化测试 `33/33 CHECK_OK`；真实模型与 iPhone 验收仍为 `NOT_RUN`。

---

## 12. 主要问题与解决结果速览

| 问题 | 原因 | 解决方式 | 当前结果 |
|---|---|---|---|
| `INVALID_TIME` | Shortcut 时间格式或模型时间不符合边界 | 统一带时区 ISO 8601，并用本地规则处理“今晚” | 已解决 |
| Bearer Token 无效 | Shortcut 与 Windows Token/请求头不一致 | 统一 `Authorization: Bearer ...` 并复用 Header | 已解决 |
| 首轮搜索 `sources=0` | 搜索结果在 `tool_calls` 中，代码读取位置错误 | 递归提取 `search_result` | 已解决 |
| 上游超时 | 来源和摘要输入过大 | 限制来源、摘要和输入长度，设置截止时间 | 已缓解 |
| 所有候选被拒绝 | 证据门槛高于 Web Search 实际数据能力 | 可选字段缺失时省略，仅过滤明确冲突 | 已解决 |
| `ready` 但没有 Notes / `[DONE]` | 轮询未鉴权、条件和变量配置错误 | 修复 GET Header、状态文本和真实完成回调 | 历史版本已真机通过 |
| 备忘录信息冗余 | 每家重复输出未知字段 | 只展示有来源事实，统一全局说明 | 已解决 |
| 普通忌口误伤门店 | 将口味偏好当成整店冲突 | 普通忌口仅提醒点餐规避 | 已解决 |
| 大众点评补证为 0 | `site:` 不是正式域名过滤；正式过滤返回页面也可能没有相关推荐 | 0.2.1 使用 `search_domain_filter` 并以页面实际内容为准 | 已测试，暂无可用点评证据 |
| 0.2.1 首次 POST 曾卡住 | Shortcut 首个网络动作的配置或运行状态异常 | 继续核对后已恢复请求链路 | 已解决 |
| 大众点评单条评价被当作门店来源 | 0.2.1 只校验域名，未校验 URL 类型和候选归属 | 0.2.2 只收 `/shop/<id>`，匹配候选并增加 `candidate_scope` | 自动化已解决，真机待回归 |
| 指南中曾包含真实格式 Token | 用户主动将调试 Token 写入本地文档 | 交付文档替换为占位符，本机继续复用原 Token | 已按用户决定处理 |

---

## 13. 当前状态

### 已完成

- QuietBite 产品定义、开发 Spec 和 0.3.0 实现。
- Windows 后端、异步 Job、鉴权、幂等、位置脱敏、模型调用、搜索、证据校验、排序和 Notes 回调协议。
- Shortcut 配置指南、README、环境变量示例和真机验收模板。
- 自动化测试 `37/37 CHECK_OK`。
- 历史版本已真实完成搜索、Notes 创建和 `[DONE]` 回调。
- 0.2.0 已真实验证大众点评 `site:` 路径无结果，并完成安全回退。
- 0.2.1 首个“获取 URL 内容”卡住的问题已解决。
- RT-01 已由用户确认完成；详细环境、耗时和 Notes 数量仍待补录。
- 大众点评正式域名过滤已实际尝试；目标页面显示“暂无相关推荐”，本轮无可用点评字段。
- 火锅、日料、粤菜三次真实运行均创建新 Job、Notes 和 `[DONE]`，证明约束变化会重新搜索；
  但 0.2.1 返回的单条评价链接未通过人工事实核验。
- 0.2.2 已实现具体门店页过滤、候选名称匹配和来源隔离。
- 高德 Web 服务 Key 已申领并真实直调成功，POI 返回评分、人均、营业时间和电话。
- 0.3.0 已实现高德主数据链路、区县过滤、可选坐标周边搜索和 Web Search 兜底。
- 用户已完成一轮 0.3.0 真实回归，并确认所有详情均可打开、Notes 内容与来源事实一致。

### 尚未完成

- 可选补证：保存 RT-03 响应 JSON 中的 `error_code=LIVE_QUEUE_UNAVAILABLE`；用户已接受
  现有 `rejected`、无 Notes、无 `[DONE]` 的真实行为证据，因此不阻塞功能验收。
- 清理并导出最终 Shortcut。
- 1–2 分钟演示视频。
- Git 初始化、清晰 Commit、GitHub 仓库和提交链接。
- RT-01 的部分精确时间、Notes 数量与逐店字段仍未抄录，但用户已确认该案例通过。

当前准确状态：0.3.0 代码自动化为 `CHECK_OK`；RT-01、RT-02、RT-03、30 秒前台无干扰、
Notes 交付、`[DONE]` 回调和来源人工核验均由用户确认 `PASS`。因此真机功能验收为
`PASS（用户确认）`；Shortcut Release、演示视频和 GitHub 仓库仍为待完成交付材料。

---

## 14. 当前阶段的主要经验

1. 本题的核心创新不是更强的点击能力，而是避免点击前台 GUI。
2. 系统原生能力组合可以比视觉 GUI Agent 更适合“用户与 Agent 同时使用手机”的场景。
3. Spec 必须建立在真实数据可得性上，不能按理想网页字段设计证据门槛。
4. LLM 适合解析与抽取，本地代码应负责安全边界、确定性过滤和排名。
5. 自动化测试、真实模型测试、真机交付测试和人工事实核验是四种不同证据，不能互相替代。
6. 真实日志比反复修改提示词更能定位问题。
7. 失败路径也应保持可用：可选补证失败时回退，而不是让整个任务失败。
8. 来源可信度至少包含三层：域名正确、页面类型正确、页面实体与候选一致。

## 15. 后续可展开的章节

- scrcpy、AutoGLM 与 QuietBite 的架构级比较。
- 为什么 Shortcut 能做到不抢前台，以及它的系统边界。
- `request_id`、`job_id`、轮询和完成回调的完整时序。
- 位置隐私脱敏和提示注入防护。
- Web Search 响应解析与证据 Source ID 机制。
- 证据门槛从严格到务实的演变过程。
- 大众点评数据路径的技术、稳定性与合规权衡。
- 真机验收和 1–2 分钟演示脚本。
- AI Coding 协作方式、代码审查和错误复盘。

---

## 16. 从大众点评搜索补证转向高德 POI（V0.3.0）

### 本轮用户反馈

用户完成高德开放平台 Web 服务 Key 的申领和接口直调，提供了真实 PowerShell 结果：
`status=1`、`info=OK`、`count=3`，三个 POI 均直接返回门店名、地址、评分、人均、
`opentime_today` 和电话。示例中既有南山区门店，也有福田区、罗湖区门店。

### 我们的理解与决策

这组结果证明，项目此前缺少的并不是更复杂的提示词，而是稳定的结构化数据源。
因此 V0.3.0 将高德 POI 2.0 设为主数据源：

- 用户明确写出“海岸城附近”等商圈时，调用 `/v5/place/text`；
- 用户只说“当前位置附近”且 Shortcut 提供坐标时，调用 `/v5/place/around`；
- 坐标在 Windows 端先保留 3 位小数，只用于高德请求，不进入日志和 Notes；
- 评分、人均、当日营业时间和电话由本地代码直接从 `business` 映射，不再交给 LLM
  二次抽取；
- 高德结果按目标城市和区县过滤，解决直调示例中混入福田、罗湖门店的问题；
- Web Search 和严格的大众点评门店页过滤仍保留，但只作为高德无可用候选时的兼容兜底。

### 本轮实施

- 服务版本升级到 `0.3.0`，新增必填环境变量 `AMAP_WEB_KEY`；
- 增加高德文本/周边搜索、响应校验、POI 到 Candidate 的确定性转换和高德详情链接；
- 支持分段营业时间、跨午夜营业时间以及 `24:00`；
- POST `/v1/jobs` 新增可选且必须成对出现的 `latitude`、`longitude`；
- 明确商圈优先于当前位置坐标，避免用户搜索“海岸城”时错误使用手机当前点；
- 有可用高德候选时不再触发 Web Search 或大众点评补证；
- 更新 README、Spec、Shortcut 指南、环境变量示例和真机验收清单；
- 自动化由 33 项增加到 37 项，当前 `37/37 CHECK_OK`。

### 关键发现

- 大众点评搜索摘要返回“像页面的 URL”并不等于得到门店聚合数据；高德 POI 的
  `business` 字段更适合当前演示所需的评分、人均、营业时间和电话。
- POI 搜索也不能只依赖 `region=深圳市`；它会返回全市结果，必须在本地再按区县过滤。
- 结构化字段不需要 LLM 重写。让本地代码直接保留提供方数值，比继续调提示词更可靠、
  更快，也更容易测试。
- 高德 POI 与高德路线规划是两种不同能力：本版使用前者，不因此承诺距离或到达时间。

### 用户后续响应

用户随后完成了 0.3.0 真实 Shortcut 回归，并确认来源均可打开、内容属实；后续又补充
RT-02、RT-03 的真机回执。因此高德主链路和真机功能验收现均为 `PASS（用户确认）`。

### 复盘

做对的地方：在用户给出真实 API 返回后切换到结构化数据源，并把区县混入、字段丢失
和坐标隐私分别落实为代码与测试，而不是继续依赖随机搜索摘要。  
此前不足：在高德 Key 尚未取得时过早围绕大众点评搜索摘要做了多轮补证，技术上能跑通，
但事实完整度和链接可核验性不足。  
当前结果：实现与自动化检查完成，真实模型、高德主链路、Notes 和前台无干扰的组合
回归已由用户在真实 iPhone 上完成并确认通过；正式交付材料仍待整理。

---

## 17. V0.3.0 首轮事实验收回执

### 用户原始请求与反馈

> “这一次所有东西都可以正常打开，并且内容属实。那现在验收清单是不是也应该更新一下，
> 然后把需要验收的东西发给我。”

### 我们的理解

这份人工回执可以确认本轮 V0.3.0 结果的两项事实：所有门店详情链接可访问，Notes
展示内容与实际来源一致。它不能自动证明尚未描述的 RT-02、RT-03、30 秒前台无干扰
细项、Notes 数量、任务耗时和演示录像。

### 完成的工作

- 将“0.3.0 高德主链路真机回归”改为 `PASS（用户确认）`；
- 将“人工事实核验”改为 `PASS（本轮用户确认）`，明确只覆盖本轮结果；
- 原样保存用户验收回执和证据边界；
- 把已经完成的 0.3.0 启动、链接打开和事实核验从待办中勾除；
- 新增只包含当前未完成事项的验收清单，保留 RT-02、RT-03、30 秒无干扰、Shortcut
  导出、演示视频和 GitHub 交付。

### 复盘

做对的地方：接受用户真实人工核验作为真机证据，同时限定它只覆盖用户明确确认的范围。  
仍缺少的证据：具体 Job ID、逐店字段、Notes 前后数量、耗时、设备环境和录像。  
当前结论（当时）：本轮链接与事实核验 `PASS`；随后 RT-02、RT-03 也已由用户真机验证，
最新状态见下一节。

---

## 18. RT-02、RT-03 真机验收回执与功能验收结论

### 用户原始请求

> iPhone 型号：16pro  
> iOS 版本：26  
> 测试时间：现在  
> 网络类型：本地网络  
>
> RT-02 job_id：job 8427d5ef-587d-4e04-a5e1-5c72d1859f0f  
> RT-02 耗时：几秒钟  
> Notes 数量：运行前 -> 运行后 刚好多了1篇  
> 30 秒前台无干扰：PASS  
> 所有来源可打开且内容属实：PASS  
> [DONE] 正常出现：PASS  
>
> RT-03 错误码：INFO job be09e8ac-21c9-432b-bc28-5ba62de4482e client poll observed status=rejected  
> 未创建 Notes：PASS  
> 未输出 DONE：PASS  
>
> 视频文件名：晚点再录，但是作为验收通过

### Codex 的理解

这是一份真实 iPhone 的人工验收回执。用户明确接受 RT-02、RT-03 和当前真机功能验收
通过；“视频晚点再录”表示视频仍是待交付材料，但不撤销用户对功能行为的验收结论。
RT-03 提供的是 `status=rejected` 日志，而不是响应 JSON 的精确 `error_code`，因此两者
必须分开记录，不能把未截取的字段写成已验证。

### 完成的工作

- 将设备环境记录为 `iPhone 16 Pro`、`iOS 26`、`本地网络`；
- 将用户所说的“现在”落盘为 `2026-09-19T18:09:29+08:00`；
- 记录 RT-02 Job `8427d5ef-587d-4e04-a5e1-5c72d1859f0f`、几秒耗时、Notes `+1`、
  30 秒无干扰、来源核验和 `[DONE]`；
- 记录 RT-03 Job `be09e8ac-21c9-432b-bc28-5ba62de4482e` 的 `rejected`、无 Notes、
  无 `[DONE]`；
- 把真机功能验收更新为 `PASS（用户确认）`，同时把视频、Shortcut Release 和 GitHub
  继续列为待完成交付材料。

### 关键发现

- 真实手机前台连续操作 30 秒期间，后台任务能够完成并交付 Notes，核心“不抢屏幕、
  不抢焦点”目标获得了真机证据；
- RT-02 使用独立 Job 并新增恰好一篇 Notes，证明约束变化会触发真实新任务；
- RT-03 在不可验证的实时排队约束下终止为 `rejected`，且没有产生 Notes 或伪造成功；
- 精确业务错误码仍是可选补证项，当前只有外部可观察行为得到确认。

### 用户响应

用户明确表示视频稍后录制，但当前案例“作为验收通过”。因此采用用户给出的人工验收
结论，不把尚未制作的视频误写成已完成。

### 做对、做错或遗漏

做对：保留 Job ID、设备、网络、Notes 增量、前台无干扰、来源核验和回调等原始证据，
并区分“功能验收通过”与“交付材料齐全”。  
遗漏：RT-02 的精确耗时和逐店字段没有单独抄录；RT-03 没有保存响应 JSON 中的
`LIVE_QUEUE_UNAVAILABLE`；演示视频尚未录制。以上内容均如实保留，不倒推或补造。

---

## 19. Shortcut 本地导出与 GitHub 安全边界

### 用户原始请求

> “这是我导出的 shortcut，然后初始化 Git，整理 Commit 并推送 GitHub。”

### Codex 的理解

用户授权初始化仓库、整理提交并创建 GitHub 仓库。提供的 Shortcut 是当前真机 Demo
导出物；在推送前必须确认它不会把部署 Token、API Key 或本地网络配置带入版本历史。

### 完成的工作

- 核对导出文件大小与 SHA-256，并确认它是 Apple 签名的 `AEA1` 归档；
- 明文扫描没有发现 Token、API Key 或私网 IP，但 Windows 不能可靠解析并审计归档内
  的全部 Shortcut 动作和值；
- 因当前 Demo 必须持有可运行的 IP 和 Bearer Token，将 `*.shortcut` 加入 `.gitignore`，
  不把未经脱敏的签名归档写入 Git 历史；
- 仓库保留完整的手工配置与 Release 导出指南，后续可加入重新导入验证过的占位符版本；
- 在 `main` 分支初始化 Git，使用 GitHub noreply 邮箱作为本仓库提交身份；
- 按需求文档、功能实现、自动化测试、部署与验收文档拆分为四个清晰提交；
- 创建并推送私有仓库 `https://github.com/Nioo4/quietbite-agent`。

### 关键发现

导出成功不等于适合公开分发。Shortcut 的签名归档可以被 iPhone 导入执行，即使 Windows
文本扫描看不到敏感值，也不能据此证明内部没有部署凭据。

### 用户响应

当前轮次尚未收到用户对“Demo 文件不入库、Release 文件稍后补充”的进一步反馈；Git
初始化和 GitHub 推送已按原请求完成，不因此阻塞。

### 做对、做错或遗漏

做对：在首次 Git 提交前执行密钥边界检查，避免凭据即使之后删除仍留在 Git 历史。  
遗漏：尚未取得可由 Windows 审计或由 iPhone 确认已替换 IP/Token 的 Release 导出文件。
仓库当前为 private，正式提交前还需要添加评审账号或改为 public。
